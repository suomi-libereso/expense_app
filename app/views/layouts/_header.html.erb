<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <ul class="current_user_info">
          <li class="current_user_id"><%= @current_user.id %></li>
          <li class="current_user_name"><%= @current_user.name %></li>
          <li></li>
          <li><%= link_to "社員一覧", organization_users_path(@current_user.organization_id) %></li>
          </ul>
          <li><%= link_to "Log out", logout_path, method: :delete %> </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>